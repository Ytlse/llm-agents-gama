# Artefacts générés (ne pas versionner)
.pytest_cache/
__pycache__/
*.pyc

# Store SQLite + exports d'une campagne
calibration_results/
*.db
*.db-wal
*.db-shm

# Jeux gelés générés (reproductibles depuis experiments/ + manifest)
calibration_datasets/

# Config locale de run (le gabarit run.example.yaml, lui, est versionné)
run.yaml
