[ ATTENTE ] Attente à '{{ ob.stop_name }}' pour le {{ ob.by_vehicle_route_id | get_transit_route_type }} {{ ob.by_vehicle_route_id | format_route_id }}; Temps d'attente : {{ ob.duration | duration_to_bucket_text }}.{% if weather %} Météo : {{ weather.temperature | int }}°C, {{ weather.weather_label }}{% if weather.precip_mm > 0 %} — pluie dans la journée : {{ weather.precip_mm }}mm{% endif %}.{% endif %}
