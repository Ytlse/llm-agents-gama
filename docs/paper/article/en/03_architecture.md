# 3. Architecture of the agentic device

<!-- Dernière mise à jour : 2026-09-17 -->

**Document:** chapter 3 of the AAMAS 2027 paper, **first written in French**. This English version translates the French draft [`fr/03_architecture.md`](../fr/03_architecture.md) `v0.7`; from here on the two texts are held in parity, the French one remaining the source for this chapter. The LaTeX rendering for Overleaf is [`overleaf/03_architecture.tex`](../overleaf/03_architecture.tex) (`draft v0.7`, 17 September 2026).
**Status:** `draft v0.7` (17 September 2026) — English version of the French `v0.7`: Figure 2 added to § 3.4, the temporal component of the retrieval score by age, at two levels of gravity and for a concept before and after counter-examples. The decay is now said to run **from the last recall**, which the text left open while the figure carries that origin on its axis.
**Previous status:** `draft v0.6` (16 September 2026) — English version of the French `v0.6`: three borrowings from a variant of the chapter offered in review. The vehicle chain is written $C_{i,t}$ and no longer $C_i$, its position changing over the day, which is the subject of § 3.5. A paragraph in § 3.1 says how temporal causality holds across a thousand asynchronous agents, a queue served by increasing deadline and the GAMA clock held back. The draw in § 3.3 is written in two steps, the vector over the simplex and then the categorical draw. Set aside from that variant: the six-symbol tuple $\mathcal{S}_{\text{MAS}}$, half of whose symbols were never used again, and the sentence giving accidents as drawn from a historical distribution, ticket 070 being blocked with nothing written.
**Previous status:** `draft v0.5` (15 September 2026) — English version of the French `v0.5`: ticket 050 applied (the ⟨P, M, C, π⟩ tuple in § 3.1, the action space and the draw in § 3.3); the author's own pass the same day, carried over from the LaTeX — chapter and § 3.2 titles changed, lists punctuated with colons, and three passages tightened or dropped: the arrival constraint, the variable contract in § 3.3, the justification of the consolidation floor; the fixed schedule made explicit (the same chain every weekday, no activity at weekends); Figure 1 added; § 3.4 **anticipates ticket 071** (two consolidation conditions from ticket 048, episodic against semantic register, five-term retrieval, core memory) — the text runs ahead of the code, as it does for the accidents of ticket 070, and that is the intent; the fifteen-minute cap per adjustment dropped from § 3.4, the code capping at one hour.
**Previous status:** `draft v0.4` (15 September 2026) — English version of the French `v0.4` (11 September 2026): translation only, no change of substance. That French version kept the author's own rewrites; four sentences were reworded (fixed schedule, the list of contingencies, the perimeter of the survey, the opening of the paragraph on the model's output) and the variable contract was rewritten in plain terms, the earlier formulation being unintelligible.
**Version antérieure :** `draft v0.3` (11 September 2026) — return to the narrative style of `v0.1`, the corrections of `v0.2` kept. Two additions came out of a check in the code: the phrase "GAMA constrains the decision" was a shortcut — what GAMA produces is the consequences, the gap between the planned trip and the trip as lived; and the agent **adjusts its own schedule** when it arrives late, which the chapter passed over in silence.
**Version antérieure :** `draft v0.2` (11 September 2026) — author's review, fourteen points. Dropped as out of scope at eight pages: the detail of the calls to the engines, the time zone, the check that a point attaches to the graph, the school bus, the fittings of the vehicle chain. Corrected: an agent who does not drive may still be a passenger; the model returns a probability per option, not an index. · `draft v0.1` (11 September 2026) — first draft; a new chapter, neither the `article/` tree nor the frozen `v1.6` manuscript described the device under evaluation.
**Place in the paper:** Section 3 of the plan announced in 1.4 of [`../en/01_introduction.md`](../en/01_introduction.md). Progress: [`../README.md`](../README.md).
**Tag convention:** a figure written **[xx.y | exp_id]** is a placeholder, to be filled from the export of [`../../methode/experience_plan/experiments.yaml`](../../methode/experience_plan/experiments.yaml). A figure left in plain text is recomputable from a repository file; its source is given in an HTML comment next to it.
**Architecture sources:** [`docs/arch/agents-lifecycle.md`](../../../arch/agents-lifecycle.md), [`routing.md`](../../../arch/routing.md), [`memory-stm-ltm.md`](../../../arch/memory-stm-ltm.md), [`vehicle-chain.md`](../../../arch/vehicle-chain.md).
---

## 3.1 Overview

The device couples three components. A GAMA multi-agent simulation carries the world: the real geography of the Toulouse area, the networks, the clock, and the physical execution of trips. A controller carries the agent life cycle: it detects that an agent is due to leave and builds the itinerary supply actually available at that hour. A decision module carries the choice itself: it is there, and only there, that the language model intervenes.

![Architecture of the device: GAMA simulation, generative agent, memory, routing engines and LLM gateway.](../images/architecture_GAMA_Agents.jpg)

**Figure 1.** Architecture of the GAMA–generative agents coupling

The loop is closed. An idle agent triggers a planning step; it receives the itineraries that exist for that trip and spreads its preference across them; its decision goes to the simulation, which executes the trip on the network and returns what actually happened: transfers, waits, arrival time. These returns feed the agent's memory, which will weigh on the next decision.

A thousand agents decide asynchronously while the simulated clock runs. The controller therefore serves planning tasks by increasing deadline, the deadline being the simulated departure time, and holds the GAMA clock whenever the estimated time to drain the queue threatens a deadline. No agent leaves before it has decided, and no decision is taken after the fact. <!-- source: docs/arch/agents-lifecycle.md, § file EDF (ticket 010) et § contre-pression prédictive ; world.predictive_backpressure_enabled = true -->

Formally, an agent is a quadruple $\text{Agent}_i = \langle P_i, M_{i,t}, C_{i,t}, \pi_\theta \rangle$: $P_i$ the sealed sociodemographic persona, restricted to the variable contract of § 3.3; $M_{i,t}$ the two-component memory register at time $t$, a circular buffer of recent observations and a long-term vector index (§ 3.4); $C_{i,t}$ the state at time $t$ of the household vehicle chain (where the car and the bicycle are, who holds a licence, § 3.5); and $\pi_\theta$ the decision policy verbalised by the language model $\theta$. The first three terms tell agents apart; the fourth is common to them all, and it is the one the rest of the paper puts to the test.

The agent has a fixed schedule: the same chain of activities, in the same order, every weekday, closing in the evening on the return home. Weekends carry no activity, for simplicity. It must carry that schedule out with what the city offers at that hour, in that place, and with the vehicles it has to hand. Those bounds are set by the controller, before the question is ever put to the model. What the simulation adds is of another nature: it produces the consequences. The trip as lived is not the trip as planned; a missed bus, a missed transfer, a jam born of an incident, a quarter of an hour of delay are facts produced by execution, and it is these that come back into the loop. This is what separates the device from a classifier queried in a vacuum, and what makes the floor and the ceiling of Chapter 4 comparable with the agent: all three decide on the same substrate.

## 3.2 The itineraries offered

The itineraries offered to the agent come from two sources. OpenTripPlanner produces the public transport itineraries: bus, tram, metro, regional trains, intercity coaches, on the real timetables of the networks. A shortest-path computation on the OpenStreetMap graph produces the direct trips: walking, cycling, car. The perimeter is that of the reference survey, the Cerema's EMC² 2023: the 453 municipalities of the Toulouse metropolitan area. <!-- source: docs/arch/routing.md, § Vue d'ensemble -->

Not every mode is offered to everyone, and that restriction operates before the decision. Car and bicycle are only proposed if the agent owns them and they are at the point of departure. Driving further requires a licence and legal age, but an agent who does not drive may still travel by car as a **passenger** in a motorised household, and the option stays open on that footing. <!-- source: docs/arch/routing.md, § Modes disponibles par agent ; docs/arch/vehicle-chain.md, § 1 bis -->

## 3.3 The decision point

At the moment of choosing, the agent receives a short profile (first name, age, occupation, household composition, income level), the destination and its zone, the departure time, the current weather and that of the remaining slots of the day, the trips it still has to make before returning home, the memories judged relevant to this trip, and finally the numbered list of itineraries, each with its mode and the detail of its legs. What it can do is not an abstract set of modes: it is the finite set of itineraries viable at that instant, $\mathcal{A}(o_t, C_{i,t}) \subseteq \mathcal{O}(o_t)$, where $\mathcal{O}(o_t)$ is what the two engines of § 3.2 produce for the observation $o_t$, and where the inclusion becomes strict as soon as eligibility or the position of the vehicles $C_{i,t}$ removes an option (§ 3.5).

The language model does not designate an itinerary: it **spreads a probability mass over the options**, one entry per option, each with its justification. This is the policy $\pi_\theta$ over that list: it yields a vector $\mathbf{p}_t = \pi_\theta(\cdot \mid o_t, M_{i,t})$ over the simplex of viable options, where $o_t$ is the observation of the trip. The decision is a categorical draw from that vector, $a_t \sim \mathrm{Cat}(\mathbf{p}_t)$, seeded from that same context, reproducible identically, and never an $\arg\max$: this is what separates the device from a classifier, and what leaves it its individual heterogeneity. The options are presented to it in a randomised order so that their rank does not become a preference. This detail settles the nature of what we measure: what the model produces is a distribution. <!-- source: mobility_llm/src/mobility_llm/mode_choice.py (draw_index, argmax_index) ; categories/itinary_multi_agent/output_schema.json -->

## 3.4 Memory

Without memory, an agent decides identically every morning, and no dynamics can be observed. The device carries two levels of it.

Short-term memory is a circular buffer per agent, a hundred entries at most, partitioned by activity. It does not record decisions alone: it receives the physical experience the simulation produces: transfers, waits at the stop, arrival times, the events of the day. This is how lived experience enters the reasoning, and not through some description the agent would give of itself.

Consolidation fires on two conditions: as soon as the buffer reaches ten entries, and in any case once per simulated day, late in the evening. <!-- source: settings.agent.stm_reflection_min_entries = 10 ; stm_reflection_daily_floor_hour = 22 -->

It writes into long-term memory, a vector index specific to each agent, where two registers coexist without obeying the same clock. **Episodic** traces (what happened, and when) erode: their weight decays exponentially from the last recall, with a time constant of 2.8 days that a serious memory lengthens, never beyond thirty. **Concepts** (what the agent holds to be true, "this line saturates when it rains") do not erode: they live on a confidence that each observation confirms or contradicts, a fact not becoming false because ten days have passed. This is Tulving's distinction between episodic and semantic memory, and it is what settles what gets forgotten.

![The two clocks of long-term memory: the temporal component of the retrieval score by age, for an ordinary episodic trace, a serious one, and a concept before and after counter-examples.](../images/ch3_oubli.png)

**Figure 2.** The temporal component of the retrieval score, by age since last recall. Episodic traces at two levels of gravity; a concept before and after five counter-examples.

Retrieval is not a plain similarity search: it combines five terms: semantic proximity, the gravity of the memory, time decay, the match on weather, and affinity of place, time slot and purpose over an age window equal to the horizon of the experiment, sixty days at most. And the model does not receive raw memories. It receives what the agent has aggregated from them: what it does most often, what it holds to be true, what has just changed. Three memories chosen for this trip are added to that. <!-- source: docs/arch/memory-stm-ltm.md, partie III ; ticket 071 § 2.2 et § 2.4 -->

The agent does not merely remember that it arrived late: it draws a consequence from it. A late arrival brings forward its target time for that activity, by 75 % of the observed delay, never encroaching on the preceding or the following activity. The readjustment is written into its memory in the first person, so that it can read it again when deciding tomorrow. This is the most elementary form of learning the device carries, and it owes nothing to the language model: it is produced by the gap between the planned schedule and the schedule as lived. <!-- source: llm-agents/urban_mobility_agents/simulation_controller.py, reschedule_amount ; settings.agent.reschedule_transition_ratio = 0.75 -->

## 3.5 Trip-chain modelling

An agent who cycled to work has no car at the office in the evening. That obvious fact is not obvious to a system that would decide each trip independently, and it is what separates the device from a classifier called once per trip.

The controller therefore holds, for each agent, the position of their personal vehicles, and draws three rules from it: a vehicle mode is only proposed if the vehicle is parked at the point of departure; the vehicle of the chosen mode follows its user to the destination, the others stay where they are; and on a return trip, if a vehicle is waiting at the origin, the candidate itineraries are restricted to that mode. This last rule filters the options, it does not add a decision. <!-- source: docs/arch/vehicle-chain.md, § Les trois règles -->

The methodological consequence has to be drawn, and Chapter 6 returns to it: part of the simulated modal split is produced **under** these constraints, so as to be more representative of real situations. An agent whose car stayed at home does not choose not to drive, it cannot. This is why the floor and the ceiling of Chapter 4 are placed under exactly the same constraints: without that, we would be comparing decisions taken in different worlds.

---

### Tickets attached to this chapter
- [Ticket 050](../../../tickets/ticket_050_formalisation_mathematique_agent.md) — Mathematical formalisation of the agentic device — **applied in v0.5**: the quadruple (§ 3.1), the action space and the draw (§ 3.3).
- [Ticket 051](../../../tickets/ticket_051_reflexion_architecture_cognitive_memoire.md) — Reflection, epistemological audit and overhaul of the agents' memory architecture
- [Ticket 070](../../../tickets/ticket_070_accidents_aleatoires_sur_les_axes.md) — Accidents drawn at random on the network and the delay they inflict: the mechanism promised by "a jam born of an incident" (§ 3.1)
- [Ticket 071](../../../tickets/ticket_071_evolution_memoire_du_code_actuel_a_l_etat_vise.md) — Evolution of memory, from the current code to the target state: § 3.4 describes that target state

### Open dependency
- [Ticket 046](../../../tickets/ticket_046_asymetrie_informationnelle_du_contrat_d_evaluation.md) — **informational asymmetry of the evaluation contract.** The paragraph in § 3.3 claiming that the agent and the statistical models "see the same thing" was dropped on 15 September 2026. Chapter 3 no longer carries that claim: it remains to be handled where the evaluation contract is stated, in Chapter 4.
