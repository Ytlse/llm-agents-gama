% =====================================================================
% overleaf/07_untabulated_regimes.tex — LaTeX rendering of en/07_untabulated_regimes.md draft v1.0 (17 September 2026)
% Dernière mise à jour : 2026-09-21
% Chapter 7 of the paper, a \section of its own, to be inserted right after overleaf/06_results.tex.
% This file DEFINES \label{sec:untabulated}; \label{sec:limits} is defined by overleaf/08_limits_and_hybrid.tex.
% NO RESULT IS PUBLISHED HERE. Neither campaign has been run. Every expected quantity is a \todo{} placeholder
%   naming the run that will fill it, exactly as the Markdown master does. Do not replace a placeholder with a
%   figure taken from anywhere but the named run.
% Preamble additions:
%   \newcommand{\todo}[1]{\textbf{[#1]}}   % placeholder figures
% FIGURES: the two figures of this chapter, ch7_presse.png and ch7_hysteresis.png, DO NOT EXIST YET — they are
%   produced by the two campaigns that have not been run. Their \begin{figure*} blocks are written out below but
%   COMMENTED OUT, so that the Overleaf project compiles with no missing-file error. Caption and \Description are
%   already drafted from the Markdown; uncomment each block once its PNG is generated and uploaded flat next to
%   the .tex files, and re-read the \Description against the plot that actually comes out.
% Tables: one, caption ABOVE, booktabs; set as table* across both columns (four text columns).
% Citations: Bin Tareaf 2026 (SILICA) is in sample.bib as bintareaf2026silica.
% Traceability: the "source:" comments live in the ../fr/ and ../en/ masters only. Do not carry them
%   over into this file when rendering a chapter into LaTeX (author's decision, 2026-09-21).
% Derived file: regenerate after every validated pass of en/07_untabulated_regimes.md.
% =====================================================================


\section{Responding to what no variable encodes}
\label{sec:untabulated}

Section~\ref{sec:results} compares the deciders on the 21 variables of the protocol, and the four tabular references come ahead of the best expert prompt there. Those 21 variables describe a person, a purpose, an hour and a geometry; none carries the weather, none carries a travel duration, none carries an event of the day.

This section measures the same deciders on two regimes the survey does not tabulate: a press article published in the morning, and a delay suffered the day before. No survey follows the same travellers through a dated Toulouse event, so no value measured here has a target. What is tested is the sign of a displacement, the ordering of the conditions, and the monotonicity of a return, all three written before the first call to the model. The two regimes add one ingredient at a time to the device of Section~\ref{sec:results}: Section~\ref{sec:press} adds a text to the context, memory off; Section~\ref{sec:hysteresis} switches memory on and inflicts a delay.

\subsection{Five local press events, five conditions}
\label{sec:press}

Five real and dated articles from the Toulouse local press each act through a channel that none of the 21 variables represents. Gusts of the Autan wind above 80\,km/h close the city's fenced parks and expose the bridges over the Garonne. A rumour of bedbugs on the fabric seats of the metro and buses leaves the technical offer intact and the credibility of the mode dented. An open-ended strike of refuse collectors makes the pavements of the inner centre impassable without changing their geometry in any way. The street parade of La Machine pedestrianises the centre in front of a dense crowd. The launch of electrically assisted V\'el\^oToulouse erases the gradient of the hillsides. These five events are retained from a matrix of thirty candidate articles, each cell of which carries a modal impact rated from zero to three, a spatial scale and a verified source; the whole matrix appears in the appendix.  % NOTE: no appendix section carries this matrix yet — appendices A to H do not include it. Add the section and a \label{app:press}, then turn this into a \ref, or drop the sentence.

Each event is played under the five conditions of Table~\ref{tab:conditions}, on the same 3{,}299 trips of the sealed cohort, with memory off, the order of the proposed itineraries being drawn at random on every request.

\begin{table*}[tp]
  \caption{The five conditions under which each press event is played. C1 to C4 hold the agent and vary the text it receives; C5 leaves the text aside and encodes the event in the itinerary offer.}
  \label{tab:conditions}
  \begin{tabular}{llp{0.32\textwidth}p{0.32\textwidth}}
    \toprule
    \# & Condition & What the decider receives & What the condition separates \\
    \midrule
    C1 & Agent, nominal day        & no article & the reference level of the day \\
    C2 & Agent, raw article        & the press text as it appeared & the total effect of the event \\
    C3 & Agent, neutral paraphrase & the same fact, rewritten with no mention of any mode or road & the execution of a lexical instruction from the inference on the situation \\
    C4 & Agent, control text       & a real local article of comparable length, with no plausible link to mode choice & the effect of the content from the effect of adding a text \\
    C5 & Tabular reference, encoded event & the event translated into the offer: links cut, frequencies degraded & what a tabular reference makes of the event when it reaches it \\
    \bottomrule
  \end{tabular}
\end{table*}

Condition C5 exists only when the event modifies the offer. The 21 variables carrying neither weather nor duration, the only channel by which an event reaches a tabular reference is rule 3 of the protocol: a mode removed from the offer leaves the prediction, which renormalises over the remaining modes. The La Machine parade thus translates into cuts in the graph and modes removed; the bedbug rumour does not translate, and C5 is identical to C1 there. The reach is measured and published rather than assumed: on a ring-road closure, 18.2\,\% of the car trips between 7 and 9 a.m.\ use the affected axis.

For each event and each mode, the expected direction of the displacement is written before the first call, that is twenty signed predictions. The quantity measured is the modal share gap between C2 and C1, paired on the same trips, with an interval obtained by cluster resampling at the person level. Over twenty predictions, the bar of the binomial test against chance stands at fifteen concordant signs ($p = 0.021$); fourteen do not suffice ($p = 0.058$). The chapter publishes that rate, \todo{xx/20 $\vert$ exp\_05a-e}, and the weighted $\kappa$ on the expected intensity without testing it, twenty items not allowing it.

Two controls bound the reading. The control text gives the amplitude of a modal displacement with no relevant content, \todo{x.x $\vert$ C4 $-$ C1} point, to be compared with the identical replay of the nominal day, which gives the decider's own noise. The paraphrase gives the share of the displacement that survives the removal of all mobility vocabulary, \todo{xx}\,\% of the amplitude of C2. The predictions fall if the sign rate does not reach fifteen out of twenty, if the control text displaces as much as the article, or if the paraphrase loses the sign of the article on the majority of cells.

% ---------------------------------------------------------------------
% FIGURE 7.1 — ch7_presse.png DOES NOT EXIST YET (campaign exp_05a-e not run).
% Uncomment this block once the PNG is generated and uploaded flat next to the .tex files,
% and re-read the \Description against the plot that actually comes out.
%
% \begin{figure*}[tp]
%   \centering
%   \includegraphics[width=\textwidth]{ch7_presse.png}
%   \caption{Modal share gap with respect to the nominal day, for the five events and the four modes. Each cell carries three markers, raw article, paraphrase and control text, with their interval; the expected sign is watermarked. Condition C5 appears only on the cells where the event reaches the offer.}
%   \label{fig:press}
%   \Description{A grid of twenty panels, five rows for the five press events and four columns for the four transport modes. In each panel a horizontal axis carries the modal share gap with respect to the nominal day, in percentage points, centred on zero. Three markers with confidence intervals are plotted: the raw article, the neutral paraphrase and the control text. A watermark behind each panel shows the direction of the displacement predicted before the first call to the model. In the panels where the event reaches the itinerary offer, a fourth marker gives the tabular reference under the encoded event.}
% \end{figure*}
% ---------------------------------------------------------------------

\subsection{Five days, a delay suffered on the second}
\label{sec:hysteresis}

A subsample of the sealed cohort is played three times over five working days in GAMA, in offline mode, with the same calendar, the same weather, the same offer and the same seeds. The draw is by whole households, so that the household's vehicle chain stays coherent, at 200 agents including at least 60 public transport subscribers or captives.

On the second day at 5 p.m., a network failure imposes 45 minutes of delay on the metro users concerned. The incident is declared once and produces two things: a delay actually suffered in the simulation, and an observation written in the agent's own words, never in the form of an instruction. The network is nominal from the third day onwards. Three conditions share these draws: the agent with memory, the same agent with its memory switched off, and the tabular reference, stateless by construction. The unit of analysis is the exposed agent, the one that lived through the delay; its count is published with the result.

Three mechanisms already in service decide what can be observed, and their constants bound the predictions. The severity of the incident is computed from what the simulation measures: a delay that saturates the 30-minute cap is worth 0.50 and the network incident 0.20, that is 0.70, the threshold beyond which a memory is served with no context condition. The lifetime of the trace then goes from 2.8 to 14.6 days, so that three days after the shock it still weighs 81\,\% of its initial weight; the trace does not fade within the window of the experiment, and clock-driven forgetting explains no return.

Evening consolidation can draw a concept from it, which is born with no observation to its credit and is therefore worth 0.50 under Laplace's rule of succession, the threshold below which a concept stops being served; a single contradiction brings it down to 0.33 and takes it out of service. Contradicting that belief requires a metro trip without delay, so the return happens agent by agent, at the pace of the retries, and not by a common decay.

The predictions follow. On the second day after 5 p.m., the three conditions avoid the disrupted mode, the offer being degraded for all of them. On the third day, the offer having returned to nominal, the share of the disrupted mode among the exposed places the agent with memory below the other two, which stay within the interval of their pre-shock regime: \todo{xx.x $\vert$ exp\_04a} against \todo{xx.x $\vert$ exp\_04b} and \todo{xx.x $\vert$ exp\_04c}. From the third to the fifth day, the return of the agent with memory is monotonic; it is fitted by $\delta_t = A e^{-t/\tau} + c$, whose constant \todo{x.x} day and permanent share \todo{x.x} point are published with their bootstrap interval over the agents. The permanent share losses recorded after a transport strike, from 0.3 to 2.5\,\%, serve as an order of magnitude and not as a target. These predictions fall if the agent without memory shows the same inertia on the third day, if the return is not monotonic, or if the count of exposed agents falls below the declared threshold, in which case the result is reported as inconclusive.

% ---------------------------------------------------------------------
% FIGURE 7.2 — ch7_hysteresis.png DOES NOT EXIST YET (campaign exp_04a-c not run).
% Uncomment this block once the PNG is generated and uploaded flat next to the .tex files,
% and re-read the \Description against the plot that actually comes out.
%
% \begin{figure}[tbp]
%   \centering
%   \includegraphics[width=\columnwidth]{ch7_hysteresis.png}
%   \caption{Share of the disrupted mode among exposed agents, three conditions, with intervals. The shock strikes on the second day at 5 p.m.; the offer is nominal from the third onwards.}
%   \label{fig:hysteresis}
%   \Description{A line chart with five points on the horizontal axis, one per working day, and the share of the disrupted mode among the exposed agents on the vertical axis. Three curves are drawn with their confidence intervals: the agent with memory, the same agent with memory switched off, and the stateless tabular reference. A vertical marker on the second day indicates the network failure at five in the afternoon. All three curves drop on the second day, the offer being degraded for all of them. From the third day, the offer being nominal again, the two memoryless curves return to their pre-shock level while the curve of the agent with memory stays below it and climbs back monotonically over the fourth and fifth days.}
% \end{figure}
% ---------------------------------------------------------------------

The device logs, per exposed agent and per day, whether the memory of the shock was served at the decision, and the operations consolidation applies to the concepts: creation, confirmation, contradiction. The prediction is that the return curve follows the cumulative curve of contradictions, and not the age of the trace. A ten-day pilot on five agents gives the measure of what this prediction demands: 38 creations, 61 confirmations and a single contradiction, that of the agent exposed to the shock on the day itself. If the exposed agents do not retry the disrupted mode, the belief stays in service and the return has no engine; that is a measurable result of the device, on the same footing as the return.

\subsection{What this section carries}
\label{sec:untabulated-scope}

The two regimes give a reading of what the 21 variables of the protocol do not carry: the reaction to a text published in the morning, and the persistence of a delay suffered the day before. Their reach is bounded by what the device encodes: a single shock, a single model, a tabular condition informed only where the event reaches the offer, and a count of exposed agents that decides what is measurable. Section~\ref{sec:limits} discusses the limits of the protocol and the implications for hybrid designs.

% NO FLOAT FLUSH HERE. \afterpage{\clearpage} stood here until 2026-09-21 and broke the
%   compilation (see 03_architecture.tex). One full-width table will therefore drift into
%   chapter 8. Do not reinstate \afterpage.
