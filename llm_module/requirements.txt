# Dépendances runtime du gateway LLM — miroir de pyproject.toml [project.dependencies].
# Utilisé par le Dockerfile pour la couche de cache pip ; la source canonique est pyproject.toml.
fastapi>=0.115,<0.116
uvicorn>=0.34
celery>=5.4
redis>=5.0
httpx>=0.27
jinja2>=3.1.4
pydantic>=2.9,<3
pydantic-settings>=2.9,<3
loguru>=0.7,<0.8
prometheus-client>=0.20
PyYAML>=6.0
demjson3>=3.0.6,<4.0

# Monitoring Celery (service flower du docker-compose, même image)
flower>=2.0
