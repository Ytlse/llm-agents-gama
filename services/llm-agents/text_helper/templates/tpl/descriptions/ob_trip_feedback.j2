[ ARRIVAL ] Arrived for {{ ob.purpose }}; Actual duration: {{ ob.duration | duration_to_bucket_text }};{%- if ob.late > 0 %} Late by: {{ ob.late | duration_to_bucket_text }}. The schedule may need adjusting. {% elif ob.late == 0 %} On time. {% else %} Early by: {{ (-ob.late) | duration_to_bucket_text }}. {% endif %}{% if weather %} Weather: {{ weather.temperature | int }}°C, {{ weather.weather_label }}{% if weather.precip_mm > 0 %} — rain during the day: {{ weather.precip_mm }}mm{% endif %}.{% endif %}
