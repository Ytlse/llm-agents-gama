[ ARRIVÉE ] Arrivé·e pour {{ ob.purpose }}; Durée réelle : {{ ob.duration | duration_to_bucket_text }};{%- if ob.late > 0 %} En retard : {{ ob.late | duration_to_bucket_text }}. Il faudrait peut-être ajuster le planning. {% elif ob.late == 0 %} À l'heure. {% else %} En avance : {{ (-ob.late) | duration_to_bucket_text }}. {% endif %}{% if weather %} Météo : {{ weather.temperature | int }}°C, {{ weather.weather_label }}{% if weather.precip_mm > 0 %} — pluie dans la journée : {{ weather.precip_mm }}mm{% endif %}.{% endif %}
