[ {{ 'CAR' if ob.mode == 'car' else 'BIKE' }} ] Travelled {{ ob.distance | humanize_distance }} {{ 'by car' if ob.mode == 'car' else 'by bike' }}. Duration: {{ ob.duration | duration_to_bucket_text }}.{% if weather %} Weather: {{ weather.temperature | int }}°C, {{ weather.weather_label }}{% if weather.precip_mm > 0 %} — rain during the day: {{ weather.precip_mm }}mm{% endif %}.{% endif %}
