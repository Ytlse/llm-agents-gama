[ MARCHE ]{%- if ob.is_arrival %} Arrivé·e à destination à pied, fin du trajet.{% else %} Marché jusqu'à '{{ ob.destination_name }}'.{% endif %} Durée : {{ ob.duration | duration_to_bucket_text }}.{% if weather %} Météo : {{ weather.temperature | int }}°C, {{ weather.weather_label }}{% if weather.precip_mm > 0 %} — pluie dans la journée : {{ weather.precip_mm }}mm{% endif %}.{% endif %}
